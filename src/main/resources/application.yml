server:
  port: 8080

spring:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 15MB

  mvc:
    static-path-pattern: '/assets/**'
  resources:
    static-locations: 'classpath:/assets/'


upload:
  pic-dir: file:./upload